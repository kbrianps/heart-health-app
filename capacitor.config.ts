import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kbrianps.heart_health_app',
  appName: 'Saúde Cardíaca',
  webDir: 'dist',
};

export default config;
